How to:

 - Needed libraries
libfreetype6
libfreetype6-dev

 - Compilation
mkdir build
cd build/
cmake ../. -DCMAKE_INSTALL_PREFIX=bin/ 
make
make install

 - Usage
xsubInsertion configFile fileAviIn fileAviOut

